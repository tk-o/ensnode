/**
 * Re-export the ENSIndexer Schema from `@ensnode/ensdb-sdk` so that handlers
 * can import it without a direct dependency on Ponder's virtual modules.
 */
export * as ensIndexerSchema from "@ensnode/ensdb-sdk/ensindexer-abstract";
